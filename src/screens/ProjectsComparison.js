import React from 'react'
import {
  Text,
  View,
  StyleSheet,
  FlatList,
  TouchableOpacity,
  Dimensions,
  Button,
} from "react-native";

export const ProjectsComparison = ({}) => {
  return (
    <View></View>
  )
}
